<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/background" /> <!-- Màu nền trắng -->
    <corners android:radius="8dp" /> <!-- Bo góc -->
    <stroke
        android:width="2dp"
        android:color="@color/background" /> <!-- Viền mỏng màu xám -->
</shape>